@model Model.ViewModel.LocalidadesDTO

@Html.Hidden("RedirectTo", Url.Action("Index", "Localidades"))
<h4>Detalle de información de la localidad</h4>

<div class="box" style="padding:15px">
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.nombre)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.nombre)
        </dd>

    </dl>
</div>

<script type="text/javascript" charset="UTF-8">
   $(document).ready(function() {
        if ("!@string.IsNullOrEmpty(ViewBag.message)") {
            $.notify.defaults({ className: "@ViewBag.level" });
            $.notify(
                "@Html.Raw(ViewBag.message)",
                {
                    position: "top Center"
                }
            );
      }
    });

    $('#mdlCtrl').on('hidden.bs.modal', function () {
        var url = $("#RedirectTo").val();
        ActivarLoader();
         window.location.href = url;
    })
</script>


<script>
    $(document).ready(function () {
        $('[data-toggle="tooltip"]').tooltip();
    });
</script>


