function DatosContacto(nombre, celular, telefono, correoelectronico) {
    this.nombre = nombre;
    this.celular = celular;
    this.telefono = telefono;
    this.correoelectronico = correoelectronico;
}

function DatosEncabezado(nombreEncabezado) {
    this.nombreEncabezado = nombreEncabezado;
}


function BlobFile(blobFile, origen)
{
    this.blobFile = blobFile;
    this.origen = origen;
}