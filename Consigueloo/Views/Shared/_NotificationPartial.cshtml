
    <ul class="nav navbar-nav navbar-left" id="notificacion" >
     
      
    </ul>
<script>

    $(document).ready(function () {
        $.ajax(
            {
                type: "POST",
                url: '@Url.Action("verificarNotificaciones", "Anuncios", new { area = "Anuncios" })',
                datatype: "json",
                contentType: 'application/json',
                success: function (res) {
                    $("#notificacion").html(res);
                    DesactivarLoader();

                }

            });
    });

    
</script>