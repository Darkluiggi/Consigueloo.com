
    <ul class="nav navbar-nav navbar-left">
     
        <li class="nav-item dropdown no-arrow mx-1">
            <a class="nav-link dropdown-toggle" href="#" onclick="quitarNotificacion()" id="alertsDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="fas fa-bell fa-fw color-blue"></i>
                <!-- Counter - Alerts -->
                <span class="badge badge-danger badge-counter" id="counter">1</span>
            </a>
            <!-- Dropdown - Alerts -->
            <div class="dropdown-list dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="alertsDropdown">
                <h6 class="dropdown-header">
                    Notificaciones
                </h6>
                <a class="dropdown-item d-flex align-items-center" href="#">
                   
                    <div>
                        <div class="small text-gray-500">December 12, 2019</div>
                        <span class="font-weight-bold" id="notificacion">No tienes notificaciones pendientes</span>
                    </div>
                </a>
                
            </div>
        </li>
    </ul>
<script>

    $(document).ready(function () {
        var notificaion = document.getElementById('notificacion');
        $.ajax(
            {
                type: "POST",
                url: '@Url.Action("verificarNotificaciones", "Anuncios", new { area = "Anuncios" })',
                datatype: "json",
                contentType: 'application/json',
                success: function (res) {
                    console.log(res);
                    notificaion.innerHTML = res;

                }

            });
    });

    function quitarNotificacion(){

        var notificacion = document.getElementById('counter');
        notificacion.hidden=true;
    }
</script>